TODO:
- Player
- Test new song submission
- Cleanup tag data (most recent don't have music prop)
- Push user favorites?
- New user creation
- Abstract spotify parses and ajax calls to service or model
- Properly initialize userProfiles, still needs that bullshit first get
- Auth control of all routes
- Error system?
- Last should be the component itself
- Or maybe the tag artist columns


Using github:

1) Make changes
2) Git add -A
3) Git commit -m 'commit message'
4) git push -u origin master

Different model hooks:

model () {
	return this.store.findAll('music').then(function(music){
		return music;
	});
},
afterModel (model, transition) {
	return Ember.RSVP.all(model.getEach('comments').then(function(){
		return model;
	}));
}

model () {
	return Ember.RSVP.hash({
		songs: this.store.findAll('music'),
		comments: this.store.findAll('comment')
    }).then(function(results){
    	console.log(results);
    	return results.songs;
    });
}