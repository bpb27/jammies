import { shortDate } from '../../../helpers/short-date';
import { module, test } from 'qunit';

module('Unit | Helper | short date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shortDate(42);
  assert.ok(result);
});
